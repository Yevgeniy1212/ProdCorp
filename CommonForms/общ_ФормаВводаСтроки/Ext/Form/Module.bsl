
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Параметры.Свойство("Строка",СтрокаВвода);
	Если Параметры.Свойство("Подсказка") Тогда
		Заголовок = Параметры.Подсказка;
		АвтоЗаголовок = Ложь;
	КонецЕсли;
	Параметры.Свойство("Многострочность",Элементы.СтрокаВвода.МногострочныйРежим);
	Если Параметры.Свойство("Длина") Тогда
		Элементы.СтрокаВвода.ОграничениеТипа = Новый ОписаниеТипов("Строка",,Новый КвалификаторыСтроки(Параметры.Длина));
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ОК(Команда)
	Закрыть(СтрокаВвода);
КонецПроцедуры

&НаКлиенте
Процедура СтрокаВводаОкончаниеВводаТекста(Элемент, Текст, ДанныеВыбора, Параметры, СтандартнаяОбработка)
	Закрыть(Элементы.СтрокаВвода.ТекстРедактирования);
КонецПроцедуры
