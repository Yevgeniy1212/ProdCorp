
&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	Если ИмяСобытия = "БлокировкаИнтерфейсаВыборКлючаИСЭСФ" Тогда
		//ЭтотОбъект.Закрыть();
		ЭтаФорма.Закрыть();
	Конецесли;	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ТекстСообщения = НСтр("ru = 'Пожалуйста, подождите...'");
	Если Не ПустаяСтрока(Параметры.ТекстСообщения) Тогда
		ТекстСообщения = Параметры.ТекстСообщения;
	КонецЕсли;
	
	ПредставлениеОрганизации = "";
	Если Параметры.Свойство("ПредставлениеОрганизации", ПредставлениеОрганизации) И ЗначениеЗаполнено(ПредставлениеОрганизации) Тогда
		ТекстСообщения = ТекстСообщения + " для организации " + ПредставлениеОрганизации;
	КонецЕсли;
	
	Элементы.Декорация4.Заголовок = ТекстСообщения;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьКлюч(Команда)
	
	Закрыть(Истина);
	
КонецПроцедуры
