
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Если НЕ ЗначениеЗаполнено(ПараметрКоманды) ИЛИ ТипЗнч(ПараметрКоманды)=Тип("Строка") Тогда
		Предупреждение("Не выбран или не сформирован договор контрагента!");
		Возврат;
	КонецЕсли;
	ПараметрыФормы = Новый Структура("Договор",ПараметрКоманды);
	ОткрытьФорму("Обработка.дог_РаботаСДоговором.Форма", ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно);
КонецПроцедуры
