
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Если (НЕ ЗначениеЗаполнено(ПараметрКоманды)) ИЛИ ПараметрыВыполненияКоманды.Источник.Модифицированность Тогда
		ПоказатьПредупреждение(,"Перед расчетом модель следует записать!");
		Возврат;
	КонецЕсли;
	//Вставить содержимое обработчика.
	ПараметрыФормы = Новый Структура("Модель",ПараметрКоманды);
	ОткрытьФорму("Обработка.фин_РасчетПоМоделиПрогнозирования.Форма", ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно);
КонецПроцедуры
