#Если Клиент Тогда
	
////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ НАЧАЛЬНОЙ НАСТРОЙКИ ОТЧЕТА

// Процедура установки начальных настроек отчета по метаданным регистра накопления
//
Процедура УстановитьНачальныеНастройки(ДополнительныеПараметры = Неопределено) Экспорт
	
	// Настройка общих параметров универсального отчета
	
	// Содержит название отчета, которое будет выводиться в шапке.
	// Тип: Строка.
	// Пример:
	// УниверсальныйОтчет.мНазваниеОтчета = "Название отчета";
	УниверсальныйОтчет.мНазваниеОтчета = СокрЛП(ЭтотОбъект.Метаданные().Синоним);
	
	// Содержит признак необходимости отображения надписи и поля выбора раздела учета в форме настройки.
	// Тип: Булево.
	// Значение по умолчанию: Истина.
	// Пример:
	// УниверсальныйОтчет.мВыбиратьИмяРегистра = Ложь;
	УниверсальныйОтчет.мВыбиратьИмяРегистра = Ложь;
	
	// Содержит имя регистра, по метаданным которого будет выполняться заполнение настроек отчета.
	// Тип: Строка.
	// Пример:
	// УниверсальныйОтчет.ИмяРегистра = "";
	УниверсальныйОтчет.ИмяРегистра = "-";
	
	
	//Содержит текст построителя отчета
	
	Текст =
	"ВЫБРАТЬ
	|   Выборка.Контрагент КАК Контрагент,
	|   Выборка.Договор КАК Договор,
	|   Выборка.ВалСуммаНачОстаток КАК ВалСуммаНачОстаток,
	|   Выборка.СуммаНачОстаток КАК СуммаНачОстаток,
	|   Выборка.ВалСуммаПогашения КАК ВалСуммаПогашения,
	|   Выборка.СуммаПогашения КАК СуммаПогашения,
	|   Выборка.ВалСуммаПолучения КАК ВалСуммаПолучения,
	|   Выборка.СуммаПолучения КАК СуммаПолучения,
	|   Выборка.ВалСуммаКонОстаток КАК ВалСуммаКонОстаток,
	|   Выборка.СуммаКонОстаток КАК СуммаКонОстаток,
	|   Выборка.СуммаКурсПриПол КАК СуммаКурсПриПол,
	|   Выборка.СуммаКурсПриПог КАК СуммаКурсПриПог
	|{ВЫБРАТЬ
	|	Выборка.Организация.*,
	|	Выборка.ИсточникФинансирования.*,
	|	Договор.*}
	|ИЗ	
	|	(ВЫБРАТЬ
	|		ТиповойОстаткиИОбороты.Организация КАК Организация,
	|		ТиповойОстаткиИОбороты.ИсточникФинансирования КАК ИсточникФинансирования,
	|		ВЫРАЗИТЬ(ТиповойОстаткиИОбороты.Субконто1 КАК Справочник.Контрагенты) КАК Контрагент,
	|		ВЫРАЗИТЬ(ТиповойОстаткиИОбороты.Субконто2 КАК Справочник.ДоговорыКонтрагентов) КАК Договор,
	|		СУММА(ТиповойОстаткиИОбороты.ВалютнаяСуммаНачальныйОстатокКт)  КАК ВалСуммаНачОстаток,
	|		СУММА(ТиповойОстаткиИОбороты.СуммаНачальныйОстатокКт ) КАК СуммаНачОстаток,
	|		СУММА(ТиповойОстаткиИОбороты.ВалютнаяСуммаОборотДт) КАК ВалСуммаПогашения,
	|		СУММА(ТиповойОстаткиИОбороты.СуммаОборотДт) КАК СуммаПогашения,
	|		СУММА(ТиповойОстаткиИОбороты.ВалютнаяСуммаОборотКт) КАК ВалСуммаПолучения,
	|		СУММА(ТиповойОстаткиИОбороты.СуммаОборотКт) КАК СуммаПолучения,
	|		СУММА(ТиповойОстаткиИОбороты.ВалютнаяСуммаКонечныйОстатокКт)  КАК ВалСуммаКонОстаток,
	|		СУММА(ТиповойОстаткиИОбороты.СуммаКонечныйОстатокКт) КАК СуммаКонОстаток,
	|		СУММА(КурсовыеРазницыПриПолучении.СуммаКурсПриПол) КАК СуммаКурсПриПол,
	|		СУММА(КурсовыеРазницыПриПогашении.СуммаКурсПриПог) КАК СуммаКурсПриПог
	|	ИЗ
	|		РегистрБухгалтерии.Типовой.ОстаткиИОбороты(
	|				&ДатаНач,
	|				&ДатаКон,
	|						,
	|						,
	|				Счет В ИЕРАРХИИ (ВЫБРАТЬ РАЗЛИЧНЫЕ
	|						СписокСчетов.СчетУчета
	|					ИЗ
	|					   РегистрСведений.СчетаУчетаПоКредитамИЗаймам КАК СписокСчетов
	|					ГДЕ
	|					   СписокСчетов.ТипСчетаУчета = &ТипСчетаУчета),
	|				,
	|				{(Организация).*,
	|				(ИсточникФинансирования).*}) КАК ТиповойОстаткиИОбороты
	|
	|   Левое соединение
	|         (ВЫБРАТЬ
	|				ВЫРАЗИТЬ(ОборотыДтКт.СубконтоКТ2 КАК Справочник.ДоговорыКонтрагентов) Как Договор,
	|				Сумма(ОборотыДтКт.СуммаОборот) Как СуммаКурсПриПол
	|          Из
	|			  РегистрБухгалтерии.Типовой.ОборотыДтКт(
	|				&ДатаНач, 
	|				&ДатаКон, 
	|				, 
	|				СчетДт в ИЕРАРХИИ (&СчетаУчетаКурсРазницПоПрих),
	|				, 
	|				СчетКт В (ВЫБРАТЬ РАЗЛИЧНЫЕ
	|							СписокСчетов.СчетУчета
	|						  ИЗ
	|			   				РегистрСведений.СчетаУчетаПоКредитамИЗаймам КАК СписокСчетов
	|						  ГДЕ
	|			   				СписокСчетов.ТипСчетаУчета = &ТипСчетаУчета	
	|						 ), 
	|				,
	|				{(Организация).*,
	|					(ИсточникФинансирования).*}) КАК ОборотыДтКт
	|			СГРУППИРОВАТЬ ПО ОборотыДтКт.СубконтоКТ2
	|           ) КАК КурсовыеРазницыПриПолучении
	|     ПО  КурсовыеРазницыПриПолучении.Договор = ТиповойОстаткиИОбороты.Субконто2 
	|
	|   Левое соединение
	|         (ВЫБРАТЬ
	|				ВЫРАЗИТЬ(ОборотыДтКт.СубконтоКТ2 КАК Справочник.ДоговорыКонтрагентов) Как Договор,
	|				Сумма(ОборотыДтКт.СуммаОборот) Как СуммаКурсПриПог
	|          Из
	|			  РегистрБухгалтерии.Типовой.ОборотыДтКт(
	|				&ДатаНач, 
	|				&ДатаКон, 
	|				, 
	|				СчетДт В (ВЫБРАТЬ РАЗЛИЧНЫЕ
	|							СписокСчетов.СчетУчета
	|						  ИЗ
	|			   				РегистрСведений.СчетаУчетаПоКредитамИЗаймам КАК СписокСчетов
	|						  ГДЕ
	|			   				СписокСчетов.ТипСчетаУчета = &ТипСчетаУчета	
	|						 ), 
	|				, 
	|				СчетКт в ИЕРАРХИИ (&СчетаУчетаКурсРазницПоРасх),
	|				,
	|				{(Организация).*,
	|					(ИсточникФинансирования).*}) КАК ОборотыДтКт
	|			СГРУППИРОВАТЬ ПО ОборотыДтКт.СубконтоКТ2
	|           ) КАК КурсовыеРазницыПриПогашении
	|     ПО  КурсовыеРазницыПриПогашении.Договор = ТиповойОстаткиИОбороты.Субконто2 
	|
	|   Левое соединение
	|         (ВЫБРАТЬ
	|				ВЫРАЗИТЬ(ОборотыДтКт.СубконтоКТ2 КАК Справочник.ДоговорыКонтрагентов) Как Договор,
	|				Минимум(ОборотыДтКт.Период) Как Период
	|          Из
	|			  РегистрБухгалтерии.Типовой.ОборотыДтКт(
	|				, 
	|				&ДатаКон, 
	|				Регистратор, 
	|				СчетДт в ИЕРАРХИИ (&СчетаУчетаДС),
	|				, 
	|				СчетКт В (ВЫБРАТЬ РАЗЛИЧНЫЕ
	|							СписокСчетов.СчетУчета
	|						  ИЗ
	|			   				РегистрСведений.СчетаУчетаПоКредитамИЗаймам КАК СписокСчетов
	|						  ГДЕ
	|			   				СписокСчетов.ТипСчетаУчета = &ТипСчетаУчета	
	|						 ), 
	|				,
	|				{(Организация).*,
	|					(ИсточникФинансирования).*}) КАК ОборотыДтКт
	|			СГРУППИРОВАТЬ ПО ОборотыДтКт.СубконтоКТ2
	|           ) КАК ДанныеОПоступлении
	|     ПО  ДанныеОПоступлении.Договор = ТиповойОстаткиИОбороты.Субконто2 
	|
	|	СГРУППИРОВАТЬ ПО
	|		ТиповойОстаткиИОбороты.Организация,
	|		ТиповойОстаткиИОбороты.ИсточникФинансирования,
	|		ТиповойОстаткиИОбороты.Субконто1,
	|		ТиповойОстаткиИОбороты.Субконто2,
	//|		ТиповойОстаткиИОбороты.Валюта,
	|		ДанныеОПоступлении.Период
	|	) КАК Выборка
	|{ГДЕ
	|	Выборка.Контрагент.*,
	|	Выборка.Организация.*,
	|	Выборка.ИсточникФинансирования.*}
	|{УПОРЯДОЧИТЬ ПО
	|	Выборка.Организация.*,
	|	Выборка.ИсточникФинансирования.*,
	|	Контрагент.*}
	|ИТОГИ
	|	СУММА(ВалСуммаНачОстаток),
	|	СУММА(СуммаНачОстаток),
	|	СУММА(ВалСуммаПолучения),
	|	СУММА(СуммаПолучения),
	|	СУММА(ВалСуммаПогашения),
	|	СУММА(СуммаПогашения),
	|	СУММА(ВалСуммаКонОстаток),
	|	СУММА(СуммаКонОстаток),
	|	СУММА(СуммаКурсПриПол),
	|	СУММА(СуммаКурсПриПог)
	|ПО
	|	ОБЩИЕ
	|{ИТОГИ ПО
	|	Выборка.Организация,
	|	Выборка.ИсточникФинансирования,
	|	Контрагент,
	|	Договор}
	|АВТОУПОРЯДОЧИВАНИЕ";

	УниверсальныйОтчет.ПостроительОтчета.Текст = Текст;
	
	
	// Содержит признак необходимости вывода отрицательных значений показателей красным цветом.
	// Тип: Булево.
	// Значение по умолчанию: Ложь.
	// Пример:
	 УниверсальныйОтчет.ОтрицательноеКрасным = Истина;
	
	// Содержит признак необходимости вывода в отчет общих итогов.
	// Тип: Булево.
	// Значение по умолчанию: Истина.
	// Пример:
	// УниверсальныйОтчет.ВыводитьОбщиеИтоги = Ложь;
	
	// Содержит признак необходимости вывода детальных записей в отчет.
	// Тип: Булево.
	// Значение по умолчанию: Ложь.
	// Пример:
	// УниверсальныйОтчет.ВыводитьДетальныеЗаписи = Истина;
	
	// Содержит признак необходимости отображения флага использования свойств и категорий в форме настройки.
	// Тип: Булево.
	// Значение по умолчанию: Истина.
	// Пример:
	// УниверсальныйОтчет.мВыбиратьИспользованиеСвойств = Ложь;
	
	// Содержит признак использования свойств и категорий при заполнении настроек отчета.
	// Тип: Булево.
	// Значение по умолчанию: Ложь.
	// Пример:
	// УниверсальныйОтчет.ИспользоватьСвойстваИКатегории = Истина;
	
	// Содержит признак использования простой формы настроек отчета без группировок колонок.
	// Тип: Булево.
	// Значение по умолчанию: Ложь.
	// Пример:
	// УниверсальныйОтчет.мРежимФормыНастройкиБезГруппировокКолонок = Истина;
	
	// Дополнительные параметры, переданные из отчета, вызвавшего расшифровку.
	// Информация, передаваемая в переменной ДополнительныеПараметры, может быть использована
	// для реализации специфичных для данного отчета параметрических настроек.
	
	//УниверсальныйОтчет.ДобавитьПолеГруппировка("БазоваяЕдиницаИзмерения", "Номенклатура", "БазоваяЕдиницаИзмерения", "Базовая единица измерения");
	//
	
	//Заполнить представление полей
	//УниверсальныйОтчет.мСтруктураПредставлениеПолей.Вставить("Договор.Владелец","Заимодавец");
	
	// Заполнение начальных настроек универсального отчета
	УниверсальныйОтчет.УстановитьНачальныеНастройки(Ложь);
	
	УниверсальныйОтчет.ДобавитьПоказатель("ВалСуммаНачОстаток",   "Вал.сумма",       Истина, "ЧЦ=17; ЧДЦ=2", "НачОстаток", "Остаток на начало периода");
	УниверсальныйОтчет.ДобавитьПоказатель("СуммаНачОстаток",   "Сумма в тенге",       Истина, "ЧЦ=17; ЧДЦ=2", "НачОстаток", "Остаток на начало периода");
	
	УниверсальныйОтчет.ДобавитьПоказатель("ВалСуммаПолучения",   "Вал.сумма",       Истина, "ЧЦ=17; ЧДЦ=2", "Получено", "Получение кредитов и займов");
	УниверсальныйОтчет.ДобавитьПоказатель("СуммаПолучения",   "Сумма в тенге",       Истина, "ЧЦ=17; ЧДЦ=2", "Получено", "Получение кредитов и займов");
	УниверсальныйОтчет.ДобавитьПоказатель("СуммаКурсПриПол",   "Курсовая разница",       Истина, "ЧЦ=17; ЧДЦ=2", , );
	
	
	УниверсальныйОтчет.ДобавитьПоказатель("ВалСуммаПогашения",   "Вал.сумма",       Истина, "ЧЦ=17; ЧДЦ=2", "Погашено", "Погашение кредитов и займов");
	УниверсальныйОтчет.ДобавитьПоказатель("СуммаПогашения",   "Сумма в тенге",       Истина, "ЧЦ=17; ЧДЦ=2", "Погашено", "Погашени кредитов и займов");
	УниверсальныйОтчет.ДобавитьПоказатель("СуммаКурсПриПог",   "Курсовая разница",       Истина, "ЧЦ=17; ЧДЦ=2", , );
		
	УниверсальныйОтчет.ДобавитьПоказатель("ВалСуммаКонОстаток",   "Вал.сумма",       Истина, "ЧЦ=17; ЧДЦ=2", "КонОстаток", "Остаток на конец периода");
	УниверсальныйОтчет.ДобавитьПоказатель("СуммаКонОстаток",   "Сумма в тенге",       Истина, "ЧЦ=17; ЧДЦ=2", "КонОстаток", "Остаток на конец периода");
	
	//отборы по умолчанию
    УниверсальныйОтчет.ПостроительОтчета.Параметры.Вставить("ТипСчетаучета",Перечисления.ТипыКредитовИЗаймов.Полученные);
    УниверсальныйОтчет.ПостроительОтчета.Параметры.Вставить("СчетаУчетаДС",ПланыСчетов.Типовой.ДенежныеСредства);
    УниверсальныйОтчет.ПостроительОтчета.Параметры.Вставить("СчетаУчетаКурсРазницПоПрих",ПланыСчетов.Типовой.РасходыПоВознаграждениям);
    УниверсальныйОтчет.ПостроительОтчета.Параметры.Вставить("СчетаУчетаКурсРазницПоРасх",ПланыСчетов.Типовой.ДоходыОтКурсовойРазницы);
	
	// Добавление предопределенных группировок строк отчета.
	// Необходимо вызывать для каждой добавляемой группировки строки.
	// УниверсальныйОтчет.ДобавитьИзмерениеСтроки(<ПутьКДанным>);
	УниверсальныйОтчет.ДобавитьИзмерениеСтроки("Организация");
	УниверсальныйОтчет.ДобавитьИзмерениеСтроки("ИсточникФинансирования");
	УниверсальныйОтчет.ДобавитьИзмерениеСтроки("Договор");
	
	// Добавление предопределенных группировок колонок отчета.
	// Необходимо вызывать для каждой добавляемой группировки колонки.
	// УниверсальныйОтчет.ДобавитьИзмерениеКолонки(<ПутьКДанным>);
	
	// Добавление предопределенных отборов отчета.
	// Необходимо вызывать для каждого добавляемого отбора.
	// УниверсальныйОтчет.ДобавитьОтбор(<ПутьКДанным>);
	УниверсальныйОтчет.ДобавитьОтбор("Организация");
	УниверсальныйОтчет.ДобавитьОтбор("ИсточникФинансирования");
	
	// Добавление предопределенных полей порядка отчета.
	// Необходимо вызывать для каждого добавляемого поля порядка.
	// УниверсальныйОтчет.ДобавитьПорядок(<ПутьКДанным>);
	
	// Установка связи подчиненных и родительских полей
	// УниверсальныйОтчет.УстановитьСвязьПолей(<ПутьКДанным>, <ПутьКДаннымРодитель>);
	
	// Установка связи полей и измерений
	// УниверсальныйОтчет.УстановитьСвязьПоляИИзмерения(<ИмяПоля>, <ИмяИзмерения>);
	
	// Добавление дополнительных полей
	// Необходимо вызывать для каждого добавляемого дополнительного поля.
	// УниверсальныйОтчет.ДобавитьДополнительноеПоле(<ПутьКДанным>, <Размещение>, <Положение>);
	УниверсальныйОтчет.ДобавитьДополнительноеПоле("Договор.Владелец",ТипРазмещенияРеквизитовИзмерений.Отдельно);
	УниверсальныйОтчет.ДобавитьДополнительноеПоле("Договор.ДатаПогашения",ТипРазмещенияРеквизитовИзмерений.Отдельно);
	
КонецПроцедуры // УстановитьНачальныеНастройки()

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ ФОРМИРОВАНИЯ ОТЧЕТА 
	
// Процедура формирования отчета
//
Процедура СформироватьОтчет(ТабличныйДокумент) Экспорт
	
	// Перед формирование отчета можно установить необходимые параметры универсального отчета.
	
	УниверсальныйОтчет.СформироватьОтчет(ТабличныйДокумент);

КонецПроцедуры // СформироватьОтчет()

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ ОБЩЕГО НАЗНАЧЕНИЯ

// Процедура обработки расшифровки
//
Процедура ОбработкаРасшифровки(Расшифровка, Объект) Экспорт
	
	// Дополнительные параметры в расшифровывающий отчет можно перередать
	// посредством инициализации переменной "ДополнительныеПараметры".
	
	ДополнительныеПараметры = Неопределено;
	УниверсальныйОтчет.ОбработкаРасшифровкиУниверсальногоОтчета(Расшифровка, Объект, ДополнительныеПараметры);
	
КонецПроцедуры // ОбработкаРасшифровки()

// Формирует структуру для сохранения настроек отчета
//
Процедура СформироватьСтруктуруДляСохраненияНастроек(СтруктураСНастройками) Экспорт
	
	УниверсальныйОтчет.СформироватьСтруктуруДляСохраненияНастроек(СтруктураСНастройками);
	
КонецПроцедуры // СформироватьСтруктуруДляСохраненияНастроек()

// Заполняет настройки отчета из структуры сохраненных настроек
//
Функция ВосстановитьНастройкиИзСтруктуры(СтруктураСНастройками) Экспорт
	
	Возврат УниверсальныйОтчет.ВосстановитьНастройкиИзСтруктуры(СтруктураСНастройками, ЭтотОбъект);
	
КонецФункции // ВосстановитьНастройкиИзСтруктуры()

// Содержит значение используемого режима ввода периода.
// Тип: Число.
// Возможные значения: 0 - произвольный период, 1 - на дату, 2 - неделя, 3 - декада, 4 - месяц, 5 - квартал, 6 - полугодие, 7 - год
// Значение по умолчанию: 0
// Пример:
// УниверсальныйОтчет.мРежимВводаПериода = 1;

#КонецЕсли