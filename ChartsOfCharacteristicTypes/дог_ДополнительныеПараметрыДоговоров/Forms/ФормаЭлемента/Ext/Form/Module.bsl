
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не ЗначениеЗаполнено(Объект.Порядок) Тогда 		
		Запрос = Новый Запрос;
		Запрос.Текст =  "ВЫБРАТЬ
		                |	ЕСТЬNULL(МАКСИМУМ(ДополнительныеРеквизитыДоговоров.Порядок), 0) КАК Порядок
		                |ИЗ
		                |	ПланВидовХарактеристик.дог_ДополнительныеПараметрыДоговоров КАК ДополнительныеРеквизитыДоговоров";
		Результат = Запрос.Выполнить().Выгрузить();
		Объект.Порядок = Результат[0].Порядок + 1;		
	КонецЕсли;

КонецПроцедуры
