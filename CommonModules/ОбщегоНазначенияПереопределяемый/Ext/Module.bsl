
// Функция ПолучитьИсключенияПоискаСсылок возвращает список имен объектов метаданных,
// которые могут ссылаться на различные объекты метаданных без влияния на бизнес-логику приложения,
//
// Пример:
// Для документ "Реализация товаров и услуг" настроена подсистема версионирования объектов,
// и подсистема свойств. При этом на экземпляр документа может быть множество ссылок 
// в информационной базе (из других документов, регистров). Часть ссылок имеют значение для бизнес-логики
// (например движения по регистрам). Другая часть ссылок - "техногенные" ссылки на объект из объектов ИБ,
// которые относятся к подсистеме версионирования объектов и подсистеме свойств. Такие техногенные
// ссылки должны быть отфильтрованы в обработке удаления помеченных и при поиске ссылок на объект
// в подсистеме запрета редактирования ключевых реквизитов.
//
// Возвращаемое значение:
//  Массив       - массив строк, например, "РегистрСведений.ВерсииОбъектов".
//
Функция ПолучитьИсключенияПоискаСсылок() Экспорт
	
	Массив = Новый Массив;
	
	// Валюты
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.КурсыВалют.ПолноеИмя());");
	// Конец Валюты
	
	// ВерсионированиеОбъектов
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ВерсииОбъектов.ПолноеИмя());");
	// Конец ВерсионированиеОбъектов
	
	// Свойства
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ДополнительныеСведения.ПолноеИмя());");
	// Конец Свойства
	
	// УправлениеДоступом
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ГруппыЗначенийДоступа.ПолноеИмя());");
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.НаборыЗначенийДоступа.ПолноеИмя());");
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ЗначенияГруппДоступа.ПолноеИмя());");
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ТаблицыГруппДоступа.ПолноеИмя());");
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ПраваПоЗначениямДоступа.ПолноеИмя());");
	// Конец УправлениеДоступом
	
	// БизнесПроцессыИЗадачи
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ИсполнителиЗадач.ПолноеИмя());");
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.СписокБизнесПроцессов.ПолноеИмя());");
	// Конец БизнесПроцессыИЗадачи
	
	// ФизическиеЛица
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ДокументыФизическихЛиц.ПолноеИмя());");
	// Конец ФизическиеЛица
	
	// ПрисоединенныеФайлы
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.НаличиеПрисоединенныхФайлов.ПолноеИмя());");
	// Конец ПрисоединенныеФайлы
	
	// РаботаСФайлами
	Выполнить("Массив.Добавить(Метаданные.РегистрыСведений.ФайлыВРабочемКаталоге.ПолноеИмя());");
	// Конец РаботаСФайлами
	
	Возврат Массив;
	
КонецФункции // ПолучитьИсключенияПоискаСсылок()

