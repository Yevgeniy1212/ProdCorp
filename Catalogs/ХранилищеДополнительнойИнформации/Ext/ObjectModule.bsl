
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат
	КонецЕсли;
	#Если Клиент тогда
		Если ЭтоНовый() тогда
			Автор = ПараметрыСеанса.ТекущийПользователь;
		КонецЕсли;
		
	//	Если Автор = ПараметрыСеанса.ТекущийПользователь или Автор = справочники.Пользователи.ПустаяСсылка() тогда
	//		Если ТипЗнч(Объект) = Тип("ДокументСсылка.ЭлектронноеПисьмо") Тогда
	//			Попытка
	//				ЗначениеРазделенияДоступа = Объект.УчетнаяЗапись;
	//			Исключение
	//				ОбщегоНазначения.СообщитьОбОшибке(ОписаниеОшибки());
	//				Отказ = Истина;
	//			КонецПопытки;
	//		Иначе
	//			ЗначениеРазделенияДоступа = Справочники.УчетныеЗаписиЭлектроннойПочты.ПустаяСсылка();
	//		КонецЕсли; 
	//	Иначе
	//		Сообщить("Недостаточно прав на изменение чужих файлов");
	//		Отказ = истина;
	//	КонецЕсли;	
	#КонецЕсли
КонецПроцедуры

