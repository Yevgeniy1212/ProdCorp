#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ

Процедура НарисоватьКарту(Объект, ПолеГеографическойСхемы, КоординатыУсловий, ПраваПодписи, ВыводитьЛегенду, ЦветБлока, ЦветЛинии, ЦветТекста, ЦветСлужебногоБлока, ЦветЛинииСлужебногоБлока, ЦветТекстаСлужебногоБлока, ВысотаБлока,ШиринаБлока,РасстояниеМеждуЭтапами,СимволовВСтроке,КоличествоСтрок,Масштаб=0,ГеографическаяСхема=Ложь)  Экспорт
	
	Если ГеографическаяСхема Тогда
		 фин_Защита.НарисоватьКарту(Объект, 
		 							ПолеГеографическойСхемы, 
									КоординатыУсловий, 
									ПраваПодписи, 
									ВыводитьЛегенду,
									СтруктураЦвета(ЦветБлока),
									СтруктураЦвета(ЦветЛинии),
									СтруктураЦвета(ЦветТекста),
									СтруктураЦвета(ЦветСлужебногоБлока),
									СтруктураЦвета(ЦветЛинииСлужебногоБлока),
									СтруктураЦвета(ЦветТекстаСлужебногоБлока),
									ВысотаБлока,
									ШиринаБлока,
									РасстояниеМеждуЭтапами,
									СимволовВСтроке,
									КоличествоСтрок);
	Иначе
		 фин_Защита.НарисоватьКартуТабличныйДокумент(Объект, 
		 							ПолеГеографическойСхемы, 
									КоординатыУсловий, 
									ПраваПодписи, 
									ВыводитьЛегенду,
									СтруктураЦвета(ЦветБлока),
									СтруктураЦвета(ЦветЛинии),
									СтруктураЦвета(ЦветТекста),
									СтруктураЦвета(ЦветСлужебногоБлока),
									СтруктураЦвета(ЦветЛинииСлужебногоБлока),
									СтруктураЦвета(ЦветТекстаСлужебногоБлока),
									ВысотаБлока,
									ШиринаБлока,
									РасстояниеМеждуЭтапами,
									СимволовВСтроке,
									КоличествоСтрок,Масштаб);
	КонецЕсли;
КонецПроцедуры

Процедура НастроитьЛегенду(Схема, Видимость=Ложь, ЦветТекстаСлужебногоБлока,ГеографическаяСхема=Ложь) Экспорт
	
	Если ГеографическаяСхема Тогда
		фин_Защита.НастроитьЛегенду(Схема,Видимость,ЦветТекстаСлужебногоБлока);
	Иначе
		фин_Защита.НастроитьЛегенду_ТабличныйДокумент(Схема,Видимость,ЦветТекстаСлужебногоБлока);
	КонецЕсли;
	
КонецПроцедуры

Функция СтруктураЦвета(Цвет)
	
	Структура = Новый Структура("Зеленый, Красный, Синий", Цвет.Зеленый, Цвет.Красный, Цвет.Синий);
	Возврат Структура;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Интерфейс для работы с подсистемой Печать.

// Заполняет список команд печати.
// 
// Параметры:
//   КомандыПечати - ТаблицаЗначений - состав полей см. в функции УправлениеПечатью.СоздатьКоллекциюКомандПечати
//
Процедура ДобавитьКомандыПечати(КомандыПечати) Экспорт
	
	Возврат;
	
КонецПроцедуры      

#КонецЕсли
