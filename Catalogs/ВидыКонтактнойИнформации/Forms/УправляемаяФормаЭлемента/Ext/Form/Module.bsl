
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("Тип") Тогда
		Объект.Тип = Параметры.Тип;
	КонецЕсли;
	Если Параметры.Свойство("Вид") Тогда
		Объект.ВидОбъектаКонтактнойИнформации = Параметры.Вид;
	КонецЕсли;
	
КонецПроцедуры
