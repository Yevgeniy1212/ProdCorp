
Процедура ПередЗаписью(Отказ)
	
	Для Каждого Строка Из ОбъединяемыеДанные Цикл
		ДокументыПланированияРасходовПоступлений = Перечисления.ден_ИсточникиДанныхРасчетаПланаДвиженияДенежныхСредств.ДокументыПланированияРасходовПоступлений;
		Если Строка.ИсточникДанныхРасчета = ДокументыПланированияРасходовПоступлений И Строка.ПриходРасход.Пустая() Тогда
			ТекстСообщения = "В строке №" + Строка(Строка.НомерСтроки) + " должен быть заполнено поле ""Приход/расход"""; 			
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения,,,,Отказ);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры
