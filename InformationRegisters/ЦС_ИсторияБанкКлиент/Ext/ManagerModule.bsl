Процедура СохранитьНастройкиНаСервере(Ссылки)Экспорт
	Для Каждого строкаТЧ из Ссылки Цикл
		Если СтрокаТЧ.Выгружать тогда
			НовЗапись = РегистрыСведений.ЦС_ИсторияБанкКлиент.СоздатьМенеджерЗаписи();
			НовЗапись.ДокументСсылка = 	строкатч.документ;
			НовЗапись.Записать();	
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

