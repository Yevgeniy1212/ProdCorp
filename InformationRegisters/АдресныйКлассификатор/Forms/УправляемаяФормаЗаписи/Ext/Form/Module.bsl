&НаКлиенте
Функция ЗаполнитьКодыУровней()
	НовыйКодОбластиВКоде           = Цел(Запись.Код / куфиб_УправлениеКонтактнойИнформацией.МаскаОБласти());
	НовыйКодРайонаВКоде            = Цел(Запись.Код / куфиб_УправлениеКонтактнойИнформацией.МаскаРайона()) % 100;	
	НовыйКодНаселенногоПунктаВКоде = Цел(Запись.Код / куфиб_УправлениеКонтактнойИнформацией.МаскаГорода()) % 100000;
	Модифицировали = Ложь;
	Если Запись.КодОбластиВКоде <> НовыйКодОбластиВКоде Тогда
		Запись.КодОбластиВКоде = НовыйКодОбластиВКоде;
		Модифицировали = Истина;
	КонецЕсли;
	Если Запись.КодРайонаВКоде <> НовыйКодРайонаВКоде Тогда
		Запись.КодРайонаВКоде = НовыйКодРайонаВКоде;
		Модифицировали = Истина;
	КонецЕсли;
	Если Запись.КодНаселенногоПунктаВКоде <> НовыйКодНаселенногоПунктаВКоде Тогда
		Запись.КодНаселенногоПунктаВКоде = НовыйКодНаселенногоПунктаВКоде;
		Модифицировали = Истина;
	КонецЕсли;
	Возврат Модифицировали;
КонецФункции

//Процедура ОбновлениеОтображения()
//	
//	ЭлементыФормы.ПолныйАдрес.Заголовок = УправлениеКонтактнойИнформацией.ПолучитьПолноеНазвание(Код);
//	
//КонецПроцедуры

&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	ЗаполнитьКодыУровней();
КонецПроцедуры