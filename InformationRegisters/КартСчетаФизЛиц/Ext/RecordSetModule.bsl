
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	   	
	Для Каждого СтрокаНабораЗаписей Из ЭтотОбъект Цикл
		
		Если СтрокаНабораЗаписей.Банк <> СтрокаНабораЗаписей.НомерКартСчета.Банк Тогда
			Сообщить("Карт-счет " + СокрЛП(СтрокаНабораЗаписей.НомерКартСчета.НомерСчета) + " не может быть назначен в банке " + СтрокаНабораЗаписей.Банк, СтатусСообщения.Важное);
			Отказ = Истина;
		КонецЕсли;
	
	КонецЦикла;

КонецПроцедуры
