////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Элементы.СписокРедактировать.Видимость = РольДоступна("ПолныеПрава");
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ КОМАНД ФОРМЫ

&НаКлиенте
Процедура Редактировать(Команда)
	Элементы.СписокРедактировать.Пометка = НЕ Элементы.СписокРедактировать.Пометка;
	Элементы.Список.ТолькоПросмотр = НЕ Элементы.СписокРедактировать.Пометка;
КонецПроцедуры
