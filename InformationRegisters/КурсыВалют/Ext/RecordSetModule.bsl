
Процедура ПередЗаписью(Отказ, Замещение)
	
	#Если Клиент Тогда
		
	мВалютаРегламентированногоУчета = Константы.ВалютаРегламентированногоУчета.Получить();

	Для Каждого СтрокаНабораЗаписей Из ЭтотОбъект Цикл
		Если СтрокаНабораЗаписей.Валюта = мВалютаРегламентированногоУчета Тогда 
			Если СтрокаНабораЗаписей.Кратность <> 1 ИЛИ СтрокаНабораЗаписей.Курс <> 1 Тогда 
				Режим = РежимДиалогаВопрос.ДаНет;
				Текст = "ru = ""В поле кратность или курс введено ошибочное значение, значение должно быть равным «1». Продолжить?""";
				Ответ = Вопрос(НСтр(Текст), Режим, 60, , "Внимание!!!",); 				
				Если Ответ = КодВозвратаДиалога.Нет Тогда 
					Отказ = Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;	
		
	#КонецЕсли
   	
КонецПроцедуры
