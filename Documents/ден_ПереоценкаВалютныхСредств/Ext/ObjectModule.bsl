#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
////////////////////////////////////////////////////////////////////////////////
// ПРОГРАММНЫЙ ИНТЕРФЕЙС

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	фин_ЗаполнениеДокументов.ЗаполнитьШапкуДокумента(ЭтотОбъект);

КонецПроцедуры

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	// ПОДГОТОВКА ПРОВЕДЕНИЯ ПО ДАННЫМ ДОКУМЕНТА
	фин_ПроведениеСервер.ПодготовитьНаборыЗаписейКПроведению(ЭтотОбъект);
	Если РучнаяКорректировка Тогда
		Возврат;
	КонецЕсли;
	
	Если Не (ДенежныеСредстваВКассах ИЛИ ДенежныеСредстваНаБанковскихСчетах) Тогда
		Возврат;
	КонецЕсли; 

	ПараметрыПроведения = Документы.ден_ПереоценкаВалютныхСредств.ПодготовитьПараметрыПроведения(Ссылка, Отказ);
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	Реквизиты = ПараметрыПроведения.Реквизиты[0];
	
	// ФОРМИРОВАНИЕ ДВИЖЕНИЙ 
	ден_ПроцедурыКазначейства.СформироватьДвиженияПереоценкаВалютныхСредствДенежныеСредства(Реквизиты, ПараметрыПроведения.ТаблицаДенежныеСредства, Движения, Отказ);	
	ден_ПроцедурыКазначейства.СформироватьДвиженияПереоценкаВалютныхСредствДенежныеСредстваКПолучению(Реквизиты, ПараметрыПроведения.ТаблицаДенежныеСредстваКПолучению, Движения, Отказ);
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// СЛУЖЕБНЫЕ ПРОЦЕДУРЫ И ФУНКЦИИ	

#КонецЕсли

